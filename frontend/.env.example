# Base URL of the backend API (no trailing slash). Injected at build time.
# Local development default (if unset): http://localhost:8080
# Production example: https://your-service.onrender.com
VITE_API_BASE_URL=http://localhost:8080
